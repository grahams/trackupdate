makeM4B.py is a tool for use with trackupdate's XMLTarget plugin. After the XMLTarget plugin has made a folder, xml file, and associated image files (if specified), makeM4B stitches it all together to with the mp3 nicest archive to make a m4b file.

Use makeM4B.py -h for usage options.